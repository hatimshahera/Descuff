# @descuff/analyzer-react-vite

React/Vite static analyzer for Descuff.

This package emits Descuff-owned `StructuralAnalysis` evidence for local React/Vite apps. It is a preview analyzer and intentionally avoids backend ownership claims for frontend-only API references.
