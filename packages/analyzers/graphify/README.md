# @descuff/analyzer-graphify

Optional Graphify correlation adapter for Descuff analysis.

This is a public runtime dependency of the `descuff` CLI. Most users should install and run the CLI instead:

```bash
npx descuff start .
```

Graphify remains optional; native Descuff analysis works without this package being used directly.
