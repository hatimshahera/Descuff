# @descuff/config

Configuration loading helpers for Descuff.

This is a public runtime package used by Descuff internals. Most users should install and run the CLI instead:

```bash
npx descuff start .
```

Use this package directly only if you are building on Descuff internals.
