# @descuff/standard-webmcp

WebMCP standard adapter for Descuff.

This package handles Descuff's WebMCP planning and validation support. Most users should install and run the CLI instead:

```bash
npx descuff start .
```

Use this package directly only if you are building on Descuff internals.
