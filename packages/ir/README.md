# @descuff/ir

Descuff-owned intermediate representation contracts.

This package contains shared structural and semantic model types used across Descuff. Most users should install and run the CLI instead:

```bash
npx descuff start .
```

Use this package directly only if you are building on Descuff internals.
